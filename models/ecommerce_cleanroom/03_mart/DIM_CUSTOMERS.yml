version: 2

models:
  - name: DIM_CUSTOMERS
    columns:
      - name: customer_id
        data_tests:
          - unique
          - not_null

      - name: country
        data_tests:
          - accepted_values:
              arguments:
                values: ['CZ', 'SK', 'DE', 'PL', 'AT']