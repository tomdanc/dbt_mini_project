version: 2

models:
  - name: L02_ORDERS
    columns:
      - name: order_id
        data_tests:
          - unique
          - not_null

      - name: quality_issue
        data_tests:
          - accepted_values:
              arguments:
                values: ['negative_total']